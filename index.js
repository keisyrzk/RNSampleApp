/**
 * @format
 */

import {AppRegistry} from 'react-native';
import MainView from './App/Modules/MainView';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => MainView);
